server:
  port: 8080

spring:
  application:
    name: bookapp

  # [개발용 H2 설정] (현재 활성화 상태)
  # datasource:
  #   driver-class-name: org.h2.Driver
  #   url: jdbc:h2:mem:bookdb
  #   username: sa
  #   password: 1234
  # h2:
  #   console:
  #     enabled: true
  #     path: /h2-console
  # jpa:
  #   hibernate:
  #     ddl-auto: create
  #   show-sql: true

  # [배포 및 Aiven MySQL 설정] (Aiven이 Running이 되면 아래 주석을 풀고 위의 H2 설정을 주석처리하세요)
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://miniproject-aivle-5th-db-miniproject5th.l.aivencloud.com:27594/defaultdb?useSSL=true&requireSSL=true&verifyServerCertificate=false&serverTimezone=Asia/Seoul
    username: avnadmin
    password: AVNS_nS9anBts-2cNbAZIcp_
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
